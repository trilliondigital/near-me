import UIKit

class ViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        // This view controller is not used in the SwiftUI app
        // but is required by the storyboard
    }
}